import React from "react";
import Button from "../../shared/button";

const TextConverterPage = () => {
  return (
    <div>
      <Button text="some text" color="primary" light={true} />
    </div>
  );
};

export default TextConverterPage;
